function openDomain(domainCard) {
    url = domainCard.getAttribute("data-url");
    window.location.href = url;
}

function openCourse(courseCard) {
    url = courseCard.getAttribute("data-url");
    window.location.href = url;
}